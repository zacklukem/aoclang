def get(tup, idx) = __intrinsic__("TupleGet", tup, idx)

def is(tup) = __intrinsic__("TupleIs", tup)

def size(tup) = __intrinsic__("TupleSize", tup)

def put(tup, i, v) = __intrinsic__("TuplePut", tup, i, v)
